# Changelog

## Unreleased

### Added

- 하루에 최대 한 번 새 버전이 있는지 조용히 확인합니다. 새 버전이 있으면 팝업 위쪽 배너와(알림이 켜져 있다면) 알림센터로 한 번 알려주며, 배너에서 `brew upgrade` 명령 복사·릴리스 노트 열기·이 버전 건너뛰기를 할 수 있습니다. 설치는 앱이 하지 않고 안내된 명령을 직접 실행해야 합니다. `설정 → Data & Settings → 업데이트`에서 언제든 끌 수 있습니다.
- 진화도감에서 선택한 개체가 실제로 거쳐 온 과거 모습을 골라 팝업과 메뉴 막대 외형으로 고정할 수 있습니다. 실제 종·레벨·경험치는 그대로입니다.
- 진화도감 팝오버에서 첫 클릭이 먹지 않던 문제를 고쳤습니다. 팝오버가 열릴 때 앱을 활성화해 한 번에 눌립니다.
- 크리처가 만렙(Lv.50)에 도달하는 순간 금색 `LEVEL MAX` 축전이 한 번 뜨고, 만렙 크리처는 메인 카드의 테두리와 레벨 게이지가 금색으로 유지됩니다. 기존 Lv.51~100 크리처도 금색 표시 대상입니다.
- 변이 발동 팝업에 받아도 계승(500만 토큰)으로 같은 계보의 시작종을 새로 키울 수 있다는 안내를 추가했습니다. 변이 수락이 막다른 길로 읽히지 않게 하기 위해서입니다.

### Removed

- 진화도감의 `이 모습으로 키우기` 예약을 없앴습니다. 갈림길에 닿으면 항상 멈추고 방향을 묻고, 고른 방향은 그 진화에만 적용되어 다음 갈림길에서 다시 묻습니다. 예약해 두었던 개체는 다음 갈림길에서 선택을 묻는 상태로 돌아갑니다.

### Changed

- 실행 앱에 포함된 240종을 119개 검토 단위·190개 부모→자식 관계로 전수 검사했습니다. 독립 재심에서 실제로 연속성이 끊긴 변이체 2종만 다시 제작했으며, 최종 런타임 검토는 96점으로 통과했습니다. 단일 부모 진화는 같은 기본 체형과 최소 두 특징을 유지하고, 변이는 색상만 바꾸지 않도록 카탈로그 검증 규칙도 강화했습니다.
- 현재 최대 구성인 4단계·총 6카드·단계당 3분기 진화도감은 372×620 한 화면에 스크롤 없이 표시하며, 메인 팝업도 내용이 넘칠 때만 스크롤 탄성이 동작합니다.
- 진화도감은 현재 개체가 실제로 거쳐 온 단계만 `보유`·클릭 가능으로 표시합니다. 미래 단계와 선택하지 않은 분기는 다른 개체로 이미 발견했더라도 `미보유`·클릭 불가로 유지합니다.
- 변이 재도전과 계승이 아무 반응도 보여주지 않던 문제를 고쳤습니다. 성공하면 얻은 크리처를, 실패하면 누적 횟수와 천장까지 남은 횟수를 진화 알림과 같은 자리에 표시합니다. 그동안은 토큰만 줄고 화면이 그대로여서 버튼이 먹지 않은 것처럼 보였습니다.
- Codex 사용률(`X n%`)이 며칠치 옛 기록 사이를 오가며 튀던 문제를 고쳤습니다. 스캔 한 번이 읽는 로그 조각에 따라 지난 세션의 값이 최신값을 덮어써서, 주간 한도가 초기화돼도 옛 수치로 되돌아갔습니다. 이제 기록 시각이 더 최신인 값만 반영합니다.
- Claude 사용률(`C n%`)을 최신으로 유지하는 방식을 바꿨습니다. 0.3.0의 사용량 API 직접 조회는 Claude Code의 로그인 정보가 담긴 Keychain 항목을 읽어야 해서, macOS가 스캔할 때마다 로그인 비밀번호를 물었습니다. 이제 PunchGrow는 로그인 정보를 건드리지 않고, 60초마다 Claude Code의 상태줄 스크립트를 실행해 기존 캐시 파일이 갱신되게 합니다.

### Fixed

- 소스 이미지는 최신이지만 이미 빌드된 앱에는 오래된 크리처 이미지가 남을 수 있던 검증 사각지대를 없앴습니다. 이제 런타임 카탈로그·240개 이미지·12개 비교표의 해시와 크기를 함께 확인하고, 누락·중복·변경이 있으면 진화 시각 검증이 실패합니다.
- macOS CI가 앱을 조립한 뒤 진화 감사 원장과 22개 고의 손상 시나리오를 자동 검증해, 오래된 판정·잘못된 관계·시트 픽셀 변조·가짜 앱 경로가 배포 단계로 넘어가지 못하게 했습니다.
- 진화도감에서 과거 모습을 미리 본 뒤 메인 크리처를 좌우로 이동하면 이전 이미지·이름·등급과 새 크리처의 레벨·진화 정보가 섞이던 문제를 고쳤습니다.

## 0.3.0 — 2026-08-07

### Added

- 진화 갈림길(Lv.15·25, 평생 최대 1회·2지선다)에서 다음 모습을 직접 선택하고, 진화도감에서 "이 모습으로 키우기"로 미리 예약할 수 있습니다. 예약한 개체는 멈추지 않고 그 경로로 진화합니다.
- Lv.15 진화 순간 10% 확률로 변이가 발동하며 수락/거절을 선택합니다. 수락하면 그 모습이 최종 형태가 됩니다.
- 변이를 지나친 개체를 위한 "변이 재도전"(회당 100만 토큰 · 10% · 계보당 30회 실패 시 확정)과, 최종 진화를 마친 개체가 같은 시작종 새 개체를 얻는 "계승"(500만 토큰)을 추가했습니다.
- 같은 시작종의 중복 개체를 그룹 안에서 전환하며 각각 육성할 수 있습니다.
- 이제 240종 전부에 도달 경로가 존재합니다(일반 분기 20종은 선택, 변이 25종은 확률·재도전).

### Changed

- 메뉴 막대의 Claude 사용률(`C n%`)이 로컬 캐시 대신 Anthropic 사용량 API를 직접 조회해 최신 값을 보여줍니다. 로그인 정보는 읽기만 하고 저장하지 않으며, 조회 실패 시 기존 캐시로 폴백합니다.
- "성장 잠재력" 표시를 선택 가능 경로 기준의 "최대 도달 등급"으로 바꾸고 최소 보장 등급을 병기합니다. ORIGIN 계보 3/60(5%)은 그대로입니다.
- 진화 대기 중인 크리처는 팝업 배지로 알려주며, 만렙 대기 개체의 먹이가 헛되이 소모되지 않도록 차단합니다.
- 메뉴 막대 크리처 썸네일의 잘림과 밝기를 개선했습니다.

## 0.2.0 — 2026-08-07

### Added

- 현재 등급과 최종 성장 잠재력을 분리해 보여주는 크리처 카드와 가챠 결과를 추가했습니다.
- 직접 가챠 확률과 최종 계보 비율을 구분하는 등급표, 이미지·분기·발견 상태·레벨 조건을 보여주는 진화도감을 추가했습니다.
- 한국어·영어 사용 가이드와 실제 SwiftUI 화면 캡처를 README에 추가했습니다.
- 초기 사용량 데이터가 없는 상태를 재현하는 문서용 메뉴 캡처 모드를 추가했습니다.

### Changed

- 가챠는 60종 시작형만 획득하며 진화체는 직접 등장하지 않습니다.
- 크리처는 Lv.15·25·40에 계보를 따라 자동 진화하고 새 성장 만렙은 Lv.50입니다.
- 기존 Lv.51~100 저장 데이터는 그대로 불러오되 추가 레벨 상승은 멈춥니다.
- 기존 등급 확률 안내를 현재 크리처의 진화 단계 안내로 교체합니다.

### Fixed

- 에일루 계보가 다른 크리처로 잘못 진화하던 연결과 에일시온 이미지 품질을 수정했습니다.
- 화면 높이가 부족할 때 팝업 본문을 스크롤해 가려진 조작부에 접근할 수 있도록 수정했습니다.

## 0.1.1 — 2026-08-05

### Fixed

- 팝업 상단이 압축되는 작은 화면에서도 보유 토큰 잔액이 주간 사용률 카드에 항상 표시됩니다.
- 일반·대형 먹이를 구매하면 변경된 보유 토큰 잔액을 즉시 확인할 수 있습니다.

## 0.1.0 — 2026-08-05

PunchGrow의 첫 macOS 알파 릴리스 후보입니다.

### Added

- Claude Code와 Codex 로컬 사용량을 게임 토큰으로 적립하는 자동 수집
- 공급자가 기록한 실제 주간 플랜 사용률 `C n%` / `X n%`
- 240종 크리처와 6단계 등급, 활동 보너스, ORIGIN 천장
- 등급별 배지·테두리·오라 효과와 확률표
- 일반 먹이와 대형 먹이 구매·급여
- 클릭 및 길게 누르기 가속 연속 실행
- 로컬 저장, 백업·복원, 도감, 대표 크리처

### Performance

- 대용량 JSONL을 청크 단위로 읽는 제한 메모리 스캐너
- 공급자별 장애 격리, 증분 커서, fork replay 제거
- 실제 실행 기준 약 120MB 수준의 메모리 사용 확인

### Known limitations

- 공개 바이너리는 Developer ID 서명과 Apple 공증이 필요합니다.
- GitHub Actions의 Full Xcode 환경에서 Swift 테스트 104개와 Release 앱 빌드·서명 검증을 통과했습니다.
- 공급자가 새로운 한도 값을 기록하기 전에는 직전 플랜 사용률이 유지될 수 있습니다.
