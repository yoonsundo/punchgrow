{
  "name": "punchgrow-website",
  "version": "1.0.0",
  "private": true,
  "description": "Dependency-free static PunchGrow project site",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "validate": "node scripts/validate.mjs",
    "test": "npm run build && node scripts/validate-site.mjs dist && npm run validate",
    "preview": "node scripts/serve.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
