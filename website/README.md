# PunchGrow website

Dependency-free static project site for `punchgrow.thundo.kr`.

```bash
npm ci
npm test
npm run preview
```

`npm run build` creates the deployable site in `dist/`. Korean is served at `/` and English at `/en/`.
